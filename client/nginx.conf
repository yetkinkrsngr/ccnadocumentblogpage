server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: route everything to index.html if file not found
    location / {
        try_files $uri $uri/ /index.html;
    }
}

